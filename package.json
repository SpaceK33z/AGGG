{
  "name": "aggg",
  "version": "1.0.0",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prestart": "rm -rf dist",
    "start": "parcel ./src/index.html",
    "build": "yarn prestart && parcel build ./src/index.html --no-source-maps",
    "lint": "eslint src"
  },
  "browserslist": [
    "last 1 Safari versions"
  ],
  "devDependencies": {
    "@parcel/transformer-css": "^2.0.0-beta.1",
    "@parcel/transformer-postcss": "^2.0.0-beta.1",
    "@volst/eslint-config": "^4.1.1",
    "eslint": "^6.8.0",
    "parcel": "^2.0.0-beta.1",
    "sass": "^1.26.3"
  },
  "dependencies": {}
}
